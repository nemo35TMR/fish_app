<nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom shadow-sm py-2">
  <div class="container-fluid">
    <%= link_to root_path, class: "navbar-brand fw-semibold text-primary", data: { turbo: false } do %>
      Fish Map
    <% end %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain" aria-controls="navbarMain" aria-expanded="false" aria-label="Menu">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarMain">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-lg-center gap-lg-2">
        <li class="nav-item">
          <%= link_to "Carte", root_path, class: "nav-link", data: { turbo: false } %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <span class="nav-link small text-muted"><%= current_user.email %></span>
          </li>
          <li class="nav-item">
            <%= button_to "Déconnexion", destroy_user_session_path, method: :delete, class: "btn btn-outline-secondary btn-sm" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Connexion", new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Inscription", new_user_registration_path, class: "btn btn-primary btn-sm" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
